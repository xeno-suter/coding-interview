/**
 * Given two strings s1 and s2, return true if s2 contains the permutation of s1, or false otherwise.
 *
 * Hints: Sliding Window Algorithm
 */

function isPermutationSubstring(s1, s2) {

}

module.exports = { isPermutationSubstring }